懸念点
1. リブートスケジューラーでのリブート時のPINのステートはどのようになるか、リブート前の状態をmobes2.0側から同期するか？
2. オフライン時にinputと紐付けられたoutputは動作するか
3. PIN ControlでモーメンタリになっているPINを切り替えても元の状態に復帰しない。これは楽観的更新でValidity (ms)の時間で戻せば良いだけでは？
4. そもそもデバイス操作側のフィードバックを反映できないか？
5. ON/OFFでカードごと半透明化しているが必ずしもこのPINのON/OFFが実態の施錠、開錠、照明ON/OFFを示さないのでこれは体感と狂う
6. disabledでもカードがグレーアウトせずトグルを操作できる。これは誤った実装
7. そもそもLowでON状態のようなトグル表示になっている。
8. PIN Controlでカードにモーメンタリなどの情報も表示すべき。CHも表示すべきラベルがカード内で干渉して不要な改行が入りレイアウトが汚い、そもそもカード型である必要がないので(おそらくモバイルだと使いづらい)
   タイトル、PINmode、操作系、状態などの表示レイアウト定義して表示崩れの無いようにしてください。このPinControlは最終的にAPIから接続してこの部分だけをまとめたモバイルのコントローラサイトを作成します。
9. PIN Settingsでは例えばRate of Change (ms)やAllocation (I/O)などPINモードによっては使用しない機能はグレーアウトして入力禁止にするなどの措置が必要
10. Expiry Dateは特定の値が入ってこない場合にはデフォルト値を使うがそもそもデフォルト値をPINglobalで設定すべき内容(PWM調光の変化時間なども含め)code/ESP32/is06s/design/DesignerInstructions.mdに言及されている設定項目がない
11. リブートや各種機能の極端な同時押し、ボタン連打、極端な速度での入切り繰り返し、ネットワーク接続試行中の操作、リブート中の操作、複数アクセスによる操作など適切にデバウンス適切にフィードバックしステートをサーバー側とも同期しているか
12. そのほかパニック、ハング時に適切にWDTを阻害しないようになっているか
13. wifi接続でRSSI閾値による再接続の仕様が必要では？
14. ヒープが何かの事情で極端に圧迫された時に再起動する必要はないか？
# UI_Review1: カメラタイル表示仕様準拠設計

文書番号: Layout＆AIlog_Settings/UI_Review1
作成日: 2026-01-07
ステータス: 設計確定
関連文書: is22_14_UI改善設計_v1.1.md

---

## 1. 概要

### 1.1 目的
`is22_14_UI改善設計_v1.1.md` セクション6「カメラグリッド表示要件」と現行実装のギャップを解消し、仕様通りのカメラタイル表示を実現する。

### 1.2 対象ファイル
- `frontend/src/components/CameraTile.tsx`
- `frontend/src/components/CameraGrid.tsx`

### 1.3 カメラグリッドレイアウト仕様

#### 1.3.1 基本レイアウト
| 項目 | 仕様 |
|------|------|
| 画面占有率 | 55%（中央ペイン） |
| 列数 | 4列固定 |
| 最大段数 | 8段 |
| 最大タイル数 | 32（4列×8段） |
| 最大カメラ数 | 31台（1タイルはブランクカード用） |

#### 1.3.2 タイルサイズ仕様
| 条件 | 縦横比 | 動作 |
|------|--------|------|
| デフォルト | 1:1（正方形） | 上から順に配置 |
| 画面下端に未到達 | 1:1（正方形） | 正方形タイルで配置 |
| 画面下端に到達 | 縦幅のみ調整 | 全タイルの縦幅を動的に縮小し画面内に収める |

#### 1.3.3 マージン仕様
- 左右・上・下のマージンは同一
- 画面下端に到達した場合も下マージンを維持
- タイル間ギャップ: 8px (gap-2)

#### 1.3.4 ブランクタイル
- カメラタイルと同じサイズ
- 常にグリッド末尾に配置
- 「🔍 カメラをスキャン」ボタンを表示

### 1.4 MECEチェック
本設計は以下の観点で網羅的・相互排他的に整理されている：
- レイアウト: 画面占有率/列数/段数/マージン
- 表示項目: サムネイル/カメラ名/IP/モデル名/施設名+fid/最終更新/ステータス
- 各項目について: 仕様/現状/ギャップ/対応策 を明確化
- フォールバック: 優先順位と最終フォールバック値を明示

---

## 2. ギャップ分析

### 2.1 仕様（最終仕様: 2行表示）

```
┌─────────────────────────────────────┐
│ [サムネイル画像]               🟢  │  ← ステータスバッジ(右上)
│                                     │
├─────────────────────────────────────┤
│ 📹 フロント玄関カメラ (Tapo C100)    │  ← カメラ名 (モデル名)
│ 🌐 192.168.125.78        3分前 更新  │  ← IPアドレス | 最終更新(右寄せ)
└─────────────────────────────────────┘

※モデル名はタイルサイズによっては省略可能
※モデル名が"Unknown"の場合は括弧ごと非表示
```

### 2.2 仕様との差異一覧（修正済み）

| ID | 項目 | 仕様 | 対応状況 |
|----|------|------|----------|
| GAP-T01 | モデル名フォールバック | model→manufacturer→family→"Unknown" | ✅ 実装完了 |
| GAP-T02 | 2行表示形式 | Line1: カメラ名 (モデル名) / Line2: IP | 最終更新(右寄せ) | ✅ 実装完了 |
| GAP-T03 | 最終更新形式 | 相対時間（3分前 更新） | ✅ 実装完了 |
| GAP-T05 | タイルアスペクト比 | デフォルト1:1（正方形）、下端超過時のみ縦幅圧縮 | ✅ 実装完了 |
| GAP-T06 | グリッドレイアウト | 4列固定、最大31カメラ+1ブランク | ✅ 実装完了 |
| GAP-T04 | ステータスバッジ | 🟢オンライン/🔴オフライン/⚪無効 | ✅ 実装完了 |

---

## 3. 詳細設計

### 3.1 GAP-T01: モデル名フォールバック

#### 仕様
| 優先順位 | データソース | 例 |
|----------|-------------|-----|
| 1 | camera.model | "Tapo C100" |
| 2 | camera.manufacturer | "TP-Link" |
| 3 | camera.family | "tapo" → "Tapo" (capitalize) |
| 4 | 最終フォールバック | "Unknown" |

#### 実装コード

```typescript
// CameraTile.tsx に追加
function getModelDisplayName(camera: Camera): string {
  if (camera.model && camera.model.trim() !== '') {
    return camera.model
  }
  if (camera.manufacturer && camera.manufacturer.trim() !== '') {
    return camera.manufacturer
  }
  if (camera.family && camera.family !== 'unknown' && camera.family !== 'other') {
    // Capitalize first letter: "tapo" -> "Tapo"
    return camera.family.charAt(0).toUpperCase() + camera.family.slice(1)
  }
  return 'Unknown'
}
```

### 3.2 GAP-T02: 施設名表示

#### 仕様
- 形式: `{location} ({fid})` または `{location}`（fidがない場合）
- locationが空の場合: fidのみ表示
- 両方空の場合: "-" 表示

#### 実装コード

```typescript
// CameraTile.tsx に追加
function getLocationDisplay(camera: Camera): string {
  const location = camera.location?.trim() || ''
  const fid = camera.fid?.trim() || ''

  if (location && fid) {
    return `${location} (${fid})`
  }
  if (location) {
    return location
  }
  if (fid) {
    return `FID:${fid}`
  }
  return '-'
}
```

### 3.3 GAP-T03: 最終更新の相対時間表示

#### 仕様
- 形式: `{相対時間} 更新`（例: "3分前 更新"）
- 相対時間ルール:
  - 60秒未満: `{秒}秒前`
  - 60分未満: `{分}分前`
  - 24時間未満: `{時間}時間前`
  - 24時間以上: `{日}日前`

#### 実装
既存の `formatRelativeTime` 関数を活用し、末尾に「更新」を追加。

```typescript
// 修正: フッター表示部分
<span>{lastSnapshotAt ? `${formatRelativeTime(lastSnapshotAt)} 更新` : '--'}</span>
```

### 3.4 GAP-T04: ステータスバッジ

#### 仕様（is22_14_UI改善設計_v1.1.md セクション6.4）

| 状態 | バッジ | 条件 |
|------|--------|------|
| オンライン | 🟢 | enabled && last_verified_at が5分以内 |
| オフライン | 🔴 | enabled && last_verified_at が5分超過 |
| 無効 | ⚪ | !enabled |

#### 実装コード

```typescript
// CameraTile.tsx に追加
type CameraStatusType = 'online' | 'offline' | 'disabled'

function getCameraStatus(camera: Camera): CameraStatusType {
  if (!camera.enabled) {
    return 'disabled'
  }

  if (!camera.last_verified_at) {
    return 'offline'
  }

  const lastVerified = new Date(camera.last_verified_at).getTime()
  const now = Date.now()
  const fiveMinutesMs = 5 * 60 * 1000

  if (now - lastVerified <= fiveMinutesMs) {
    return 'online'
  }

  return 'offline'
}

function getStatusBadge(status: CameraStatusType): React.ReactNode {
  switch (status) {
    case 'online':
      return (
        <div className="w-3 h-3 rounded-full bg-green-500" title="オンライン" />
      )
    case 'offline':
      return (
        <div className="w-3 h-3 rounded-full bg-red-500" title="オフライン" />
      )
    case 'disabled':
      return (
        <div className="w-3 h-3 rounded-full bg-gray-400" title="無効" />
      )
  }
}
```

#### UIレイアウト（overlayモード）

```
┌─────────────────────────────────────┐
│ [サムネイル画像]               🟢  │  ← 右上にステータスバッジ
│                                     │
│ ──────────────────────────────────  │
│ 📹 フロント玄関カメラ               │  ← カメラ名
│ 🌐 192.168.125.78                   │  ← IPアドレス
│ Tapo C100                           │  ← モデル名(フォールバック対応)
│ HALE京都丹波口 (0150)   3分前 更新  │  ← 施設名+fid | 相対時間
└─────────────────────────────────────┘
```

---

## 4. 変更ファイル一覧

| ファイル | 変更内容 | 行数見積 |
|----------|---------|---------|
| `frontend/src/components/CameraTile.tsx` | ヘルパー関数追加 + 表示ロジック修正 | +60行, -10行 |

---

## 5. テスト計画

### 5.1 単体テスト

| ID | テスト内容 | 期待結果 |
|----|-----------|----------|
| UT-T01 | model設定済み → getModelDisplayName | modelの値を返す |
| UT-T02 | model未設定, manufacturer設定 → getModelDisplayName | manufacturerを返す |
| UT-T03 | model/manufacturer未設定, family="tapo" → getModelDisplayName | "Tapo"を返す |
| UT-T04 | 全未設定 → getModelDisplayName | "Unknown"を返す |
| UT-T05 | location+fid設定 → getLocationDisplay | "{location} ({fid})"形式 |
| UT-T06 | locationのみ → getLocationDisplay | locationのみ |
| UT-T07 | fidのみ → getLocationDisplay | "FID:{fid}"形式 |
| UT-T08 | 両方未設定 → getLocationDisplay | "-" |
| UT-T09 | enabled=true, last_verified_at=1分前 → getCameraStatus | "online" |
| UT-T10 | enabled=true, last_verified_at=10分前 → getCameraStatus | "offline" |
| UT-T11 | enabled=false → getCameraStatus | "disabled" |

### 5.2 UIテスト（Chrome）

| ID | テスト内容 | 期待結果 |
|----|-----------|----------|
| UI-T01 | カメラタイル表示確認 | 仕様通りのレイアウトで表示 |
| UI-T02 | モデル名フォールバック確認 | model未設定時にmanufacturer表示 |
| UI-T03 | 施設名+fid表示確認 | "HALE京都丹波口 (0150)"形式 |
| UI-T04 | 相対時間表示確認 | "3分前 更新"形式 |
| UI-T05 | ステータスバッジ(オンライン) | 緑色バッジ表示 |
| UI-T06 | ステータスバッジ(オフライン) | 赤色バッジ表示 |
| UI-T07 | ステータスバッジ(無効) | グレーバッジ表示 |

---

## 6. 依存関係

### 6.1 前提条件
- Camera型に以下のフィールドが存在すること（確認済み）:
  - `location: string`
  - `model: string | null`
  - `manufacturer: string | null`
  - `family: CameraFamily`
  - `fid: string | null`
  - `enabled: boolean`
  - `last_verified_at: string | null`

### 6.2 バックエンド変更
なし（既存APIで対応可能）

---

## 7. 実装タスク

| タスクID | 内容 | 見積 |
|----------|------|------|
| IMPL-T01 | getModelDisplayName ヘルパー関数追加 | 15分 |
| IMPL-T02 | getLocationDisplay ヘルパー関数追加 | 10分 |
| IMPL-T03 | getCameraStatus / getStatusBadge 関数追加 | 20分 |
| IMPL-T04 | overlay モードUI修正 | 20分 |
| IMPL-T05 | footer モードUI修正 | 15分 |
| IMPL-T06 | 相対時間表示形式修正 | 5分 |
| TEST-T01 | 単体テスト作成・実行 | 30分 |
| TEST-T02 | Chrome UIテスト実行 | 20分 |

---

## 8. 大原則チェックリスト

- [x] SSoT: Camera型の定義を唯一の情報源として参照
- [x] SOLID-S: CameraTile.tsxの責務は表示のみ
- [x] MECE: 表示項目・フォールバック・ステータス条件を網羅的に定義
- [x] アンアンビギュアス: 条件・フォールバック優先順位を明確に定義
- [x] 車輪の再発明禁止: 既存のformatRelativeTime関数を再利用
- [x] テスト計画あり: 単体テスト11項目、UIテスト7項目を定義

# IS22 Test Environment Configuration
# Copy this file to .env and fill in your values
# Usage: source .env && ./run_all_tests.sh

# Database credentials (required)
CAMSERVER_DB_USER=root
CAMSERVER_DB_PASS=your_password_here
CAMSERVER_DB_NAME=camserver

# SSH credentials for remote test execution (required for test_camera_registration_full.sh)
CAMSERVER_SSH_USER=mijeosadmin
CAMSERVER_SSH_PASS=your_password_here

# Note: Never commit the actual .env file to git
# Add .env to .gitignore
